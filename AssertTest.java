
/* codigo comentado


package calculadora;

import static org.junit.Assert.*;

import org.junit.Test;

public class AssertTest {
	
	
	@Test
	
	public void test() {
		//comprueba que dos cosas son iguales con dos parametros (resultado y esperado)
		//assertEquals(expected, actual); //si viene el tercer parametro double delta es que da un numero de rango en donde resultado y esperado sea menor o mmayor que delta
		
		//comprueba valores que sean iguales
		String  s1 = "hola";
		String s2 = "hola";
		assertEquals(s1, s2);
		assertSame(s1, s2);
		
	}

}


*/